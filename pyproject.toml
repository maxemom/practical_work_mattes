[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "practical-work-mattes"
version = "0.0.1"
requires-python = ">=3.11"

[tool.setuptools]
py-modules = []
