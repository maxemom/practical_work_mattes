paths:
  data_dir: data
  output_dir: outputs

seeds:
  seed: 42

runtime:
  device: auto

runtime:
  device: auto

generation:
  max_new_tokens: 10
  do_sample: false
  temperature: 1.0
  top_p: 1.0
