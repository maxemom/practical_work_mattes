services:
  pwm:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ../data/processed:/workspace/data/processed:ro
      - ../outputs:/workspace/outputs
    environment:
      - HF_HOME=/workspace/.cache/huggingface
      - TRANSFORMERS_CACHE=/workspace/.cache/huggingface
